marshy~=2.5
schemey~=5.4

# If using starlette (Maybe a requirement?)
starlette~=0.19

# If using jwt
pyjwt~=2.4
cryptography~=37.0

# If using aws
boto3~=1.24
mangum~=0.15

# If using http actions
requests~=2.28

# If using celery scheduler
celery~=5.2

#Dev dependency with aws
pyyaml~=6.0

# If using strawberry
strawberry-graphql~=0.125

# Only for dev if using AWS
ruamel.yaml~=0.17